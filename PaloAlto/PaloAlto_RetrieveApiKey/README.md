### - PaloAlto_RetrieveApiKey_Groovy (PropertySource)

> Generates an ApiKey within the Palo Alto endpoint & maps that property (paloalto.apikey.pass) within the device in question
> The endpoint in question needs to have API properties mapped (in order to interact with LM API - To perform PUT/POST requests)