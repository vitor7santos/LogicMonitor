### - Services (DataSource)

> Retrieves all the services (set to automatically start) via their name (not display name)
> Make use of the AD filters or create a token (Regex) to filter stuff on a resource/group level

> Supported devices:
> -
> - Windows stuff (via WMI)